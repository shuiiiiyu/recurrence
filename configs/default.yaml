model:
  num_layers: 24
  d_model: 384
  num_heads: 6
  ffn_mult: 4
  dropout: 0.0

baseline:
  description: standard causal Transformer with no depth recurrence

ratio1:
  feedback_source_layer: 24
  feedback_target_layer: 8

ratiolt1:
  entry_layers: 7
  loop_start_layer: 8
  loop_end_layer: 24
  num_loops: 2

data:
  dataset: synthetic
  sudoku_root: /data/shencanyu/data/raw/sudoku-extreme
  permutation_root: /data/shencanyu/data/raw/permutation
  permutation_subset: S3_len100_100k
  max_train_samples: null
  max_val_samples: null

train:
  batch_size: 64
  max_steps: 2000
  lr: 0.0003
  weight_decay: 0.01
